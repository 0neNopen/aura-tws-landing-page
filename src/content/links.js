/**
 * Link targets — single source of truth for every call-to-action.
 *
 * `ORDER_URL` intentionally points at the storytelling section as a safe
 * placeholder: swap it for a real checkout / pre-order URL when one exists
 * and every CTA across the page updates with it.
 */
export const ORDER_URL = '#cta';

export const FOOTER_LINKS = [
  { label: 'Privacy Policy', href: '#' },
  { label: 'Terms of Service', href: '#' },
  { label: 'Contact', href: 'mailto:hello@aura.example' },
];
